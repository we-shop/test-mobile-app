[pytest]
markers =
    uat: mark to test on uat.
    int: mark to test on int.

addopts = --driver Appium --variables caps.json -v -s --html=report.html --self-contained-html --disable-pytest-warnings